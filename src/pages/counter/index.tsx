import React from 'react';

import Counter from '../../components/counter/Counter';

const CounterView = () => {
  return (
    <div>
      <Counter />
    </div>
  );
};

export default CounterView;
